# Core runtime dependencies — the bootstrap in main.py installs these automatically.
# If you prefer manual setup, run: pip install -r requirements.txt
# Transitive dependencies will be pulled in automatically.
#
# The [voice] extra pulls PyNaCl + davey, both required by discord.py 2.7+
# for any voice feature. Without davey, joining a voice channel raises
# RuntimeError('davey library needed in order to use voice') — MusicCog's
# /play_music and friends are dead without it.
discord.py[voice]
python-dotenv
ollama
yt-dlp
pytz
PyYAML
